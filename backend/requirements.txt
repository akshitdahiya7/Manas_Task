# Web framework
fastapi==0.141.1
uvicorn[standard]==0.53.0
python-multipart==0.0.32
pydantic==2.13.5
pydantic-settings==2.15.0

# Database
SQLAlchemy==2.0.52
psycopg[binary]==3.3.5

# Auth
PyJWT==2.14.0
bcrypt==5.0.0

# ML runtime.
# scikit-learn is pinned to 1.9.0 because that is the version the supplied
# random-forest artifact was pickled with; loading it under a different
# version raises InconsistentVersionWarning and is not guaranteed to be correct.
scikit-learn==1.9.0
joblib==1.6.0
numpy==2.1.3
pandas==2.2.3

# The supplied .keras model is a plain Dense network saved with Keras 3.15, so
# it runs on Keras' inference-only numpy backend (KERAS_BACKEND=numpy) and does
# not need TensorFlow. Verified against tensorflow-cpu 2.20 on 200 random
# inputs: max absolute difference 1.19e-07, identical class decisions.
# jax is required because keras' numpy backend imports it for a few ops.
keras==3.15.0
jax[cpu]==0.10.2
